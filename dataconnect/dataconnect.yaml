specVersion: "v1beta"
serviceId: "jobwerk"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "jobwerk-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
